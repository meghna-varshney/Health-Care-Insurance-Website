import { KeyupDirective } from './keyup.directive';

describe('KeyupDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyupDirective();
    expect(directive).toBeTruthy();
  });
});
