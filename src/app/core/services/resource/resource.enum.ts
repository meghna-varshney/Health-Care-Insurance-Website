export enum HttpMethods {
  GET,
  POST,
  PUT,
  DELETE,
  PATCH
}

export enum ResponseType {
  arraybuffer,
  blob,
  json,
  text
}

export enum FreeData {
  ArrayBuffer,
  Blob,
  FormData,
  string,
  null
}
